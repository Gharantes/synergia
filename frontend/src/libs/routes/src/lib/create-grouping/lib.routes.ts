import { Route } from '@angular/router';
import { CreateGroupingComponent } from './create-grouping.component';

export const createGroupingRoutes: Route[] = [
  {
    path: '', component: CreateGroupingComponent
  },
];