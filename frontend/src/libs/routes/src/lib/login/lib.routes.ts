import { Route } from '@angular/router';
import { LoginRouteComponent } from './login-route.component';

export const loginRouteRoutes: Route[] = [
  {
    path: '', component: LoginRouteComponent
  },
];