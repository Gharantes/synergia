import { Route } from '@angular/router';
import { HomeRouteComponent } from './home-route.component';

export const homeRouteRoutes: Route[] = [
  {
    path: '', component: HomeRouteComponent
  },
];