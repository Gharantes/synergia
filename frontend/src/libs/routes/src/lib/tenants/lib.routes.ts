import { Route } from '@angular/router';
import { TenantsRouteComponent } from './tenants-route.component';

export const tenantsRoutes: Route[] = [
  {
    path: '',
    component: TenantsRouteComponent,
  },
];